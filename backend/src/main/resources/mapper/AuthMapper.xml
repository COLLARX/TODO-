<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todo.auth.AuthMapper">

    <resultMap id="userResultMap" type="com.example.todo.auth.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="userResultMap">
        SELECT id, username, password_hash, created_at, updated_at
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.example.todo.auth.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password_hash)
        VALUES (#{username}, #{passwordHash})
    </insert>

</mapper>
